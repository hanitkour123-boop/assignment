pipeline {
    agent any

    stages {
        stage('Check Branch') {
            steps {
                echo "Current branch is: ${env.BRANCH_NAME}"
            }
        }
        // other stages
    }
}

